networks:
  internal:
    driver: bridge

services:
  database:
    image: 'mariadb'
    ports:
      - '3306:3306'
    container_name: database
    networks:
      - internal
    env_file: ".env"
    volumes:
      - './data/db/:/var/lib/mysql'
