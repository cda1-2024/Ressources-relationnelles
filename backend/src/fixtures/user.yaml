entity: User
processor: ./processor/UserProcessor
items:
  user{1..50}:
    index: ($current)
    password: test
    username: '{{person.firstName}}'
    lastName: '{{person.lastName}}'
    email: '{{internet.email}}'
    country: '{{location.country}}'
    city: '{{location.city}}'
    region: '{{location.state}}'
    profile_picture: '{{internet.url}}'
    bio: '{{lorem.paragraph}}'
    createdAt: '{{date.past}}'
    updatedAt: '{{date.recent}}'
