entity: Ressources
processor: ./processor/RessourceProcessor
items:
  ressources_validate{1..20}:
    index: ($current)
    title: '{{lorem.sentence}}'
    contentText: '{{lorem.sentences}}'
    # TODO mettre autre chose
    contentLink: 'TODO_A_AJOUTER'
    createdAt: '{{date.past}}'
    creator: '@user*'
    category: '@category*'
    # On prends les uniquement les modérateurs ici
    validator: '@user{2..15}'
    dateTimeValidation: '{{date.past}}'
    state: 'published'